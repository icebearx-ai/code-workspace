# Code Workspace 宏观架构

## 架构结论

Code Workspace 采用三层能力架构：

```text
┌─────────────────────────────────────────┐
│ 第三层：Session 会话编排层              │
│ 多会话发现、持久化、状态、查询与协调    │
└──────────────────┬──────────────────────┘
                   │ 只调用 Workspace 公共契约
┌──────────────────▼──────────────────────┐
│ 第二层：Workspace 工作区控制平面        │
│ 工作区身份、项目注册、查询、授权与校验  │
└──────────────────┬──────────────────────┘
                   │ 复用基础能力
┌──────────────────▼──────────────────────┐
│ 第一层：Foundation 基础能力层           │
│ CLI、配置、事务、迁移、适配器与诊断     │
└─────────────────────────────────────────┘
```

依赖方向固定为：

```text
Session → Workspace → Foundation
```

上层可以依赖下层，下层不得反向依赖上层的领域模型。三层首先是逻辑和职责边界，不要求立即拆分为三个 npm 包或独立进程。

## 第一层：Foundation 基础能力层

第一层是现有 Code Workspace 脚手架所提供的可持续运行基础，也可以对外继续称为“脚手架层”。内部架构使用 `Foundation` 或“基础能力层”，以区别于只在项目创建时运行一次的代码生成器。

该层负责：

- CLI 命令注册、参数解析、结果和错误契约；
- 配置读取、投影、迁移和持久化；
- 操作计划、确认、事务、回滚和后置验证；
- 托管资产的安装、更新和完整性检查；
- Agent 或 provider 适配器的基础接口；
- 事件接入、国际化和其他通用基础能力。

该层不负责：

- 理解项目注册、项目分支或会话状态的业务规则；
- 直接实现 `Project` 或 `Session` 领域策略；
- 因上层需求绕过公共契约直接修改领域状态。

## 第二层：Workspace 工作区控制平面

第二层是 Code Workspace 当前的核心领域。它是本地多项目注册表和安全边界层，负责维护 Workspace 期望状态并协调 Agent 对注册项目的安全访问。

该层负责：

- Workspace 身份和配置；
- 项目注册、删除、列表和查询；
- 显式的项目作用域；
- 注册分支与实际分支的检查和协调；
- Agent 目录授权；
- 项目与 Workspace 校验、Doctor；
- 向会话层提供稳定的项目、授权和校验契约。

该层遵守以下边界：

- Workspace 是控制平面，不是生产项目；
- 不根据对话猜测项目、路径、归属或分支；
- 不直接编辑项目生产代码；
- 不创建或下载 Git 分支，不执行 fetch、stash、reset 或 commit，不处理 Git 冲突；
- 不创建、读取、更新或归档项目的 `openspec/` 记录；
- 不拥有或管理 Session 生命周期。

## 第三层：Session 会话编排层

第三层建立在 Workspace 之上，负责一个 Workspace 内的单会话和多会话管理。会话层通过 Workspace 公共契约获取项目事实、验证项目状态并请求必要授权，不直接读取或修改 Workspace 的内部存储。

该层负责：

- 会话注册、发现、列表和查询；
- 会话与 Workspace 的归属关系；
- 会话显式项目作用域；
- Session、Turn、子 Agent、等待授权等状态；
- 会话历史和事件持久化；
- 多会话监控、筛选、通知和协调；
- 在 provider 明确支持时，提供会话启动、恢复和结束等控制能力。

会话层不得：

- 从当前目录、对话内容或事件内容猜测项目作用域；
- 直接编辑 `.code-workspace/config.yaml`、权限文件或项目注册表；
- 绕过 Workspace 层执行项目授权、分支协调或项目校验；
- 将只能被观测的 provider 会话伪装为可控制会话。

## 核心对象关系

```text
一个 Workspace ──拥有──▶ 多个 Project
一个 Workspace ──拥有──▶ 多个 Session
一个 Session   ──作用于▶ 0..N 个显式选择的 Project
```

对象关系遵守以下规则：

- 一个 Session 必须且只能属于一个 Workspace；
- Session 的项目作用域可以为空，也可以包含一个或多个已注册项目，但必须显式确定；
- Session 只能引用所属 Workspace 中的注册项目；
- 会话身份使用 `(workspaceUuid, provider, nativeSessionId)` 作为稳定复合标识，避免不同 Workspace 或不同 Agent provider 的原生会话 ID 冲突；
- 项目从 Workspace 移除时，会话历史可以保留引用快照，但该项目不能继续作为可执行作用域。

## 会话观测与会话控制

会话观测和会话控制是不同能力：

- **会话观测**：接收 provider 事件，形成 Session、Turn、工具调用、子 Agent 和授权等待状态的查询投影；
- **会话控制**：通过 provider 明确支持的接口启动、恢复、停止或结束会话，并验证操作结果。

当前 monitor 已经可以按 Workspace、Session 和 Turn 聚合 Codex hook 事件，是会话观测能力的雏形，但尚不是完整会话管理。当前状态仍有以下限制：

- 会话状态主要保存在内存中，服务重启后丢失；
- 事件来源和生命周期事实依赖 provider hook；
- 缺少正式的 Session 持久化模型；
- 缺少 Session 与注册项目之间的稳定关系；
- 不具备通用的启动、恢复或结束会话能力。

## 会话层演进阶段

会话层按以下阶段演进：

1. 建立持久化的会话目录、领域模型、状态查询和多会话监控；
2. 建立 Session 与 Workspace、注册项目之间的显式关系和作用域校验；
3. 基于 provider 能力适配器增加启动、恢复、结束和协调能力；
4. 对不支持控制的 provider 保留纯观测模式，不降低事实准确性和安全边界。

第一阶段以“可靠观测和查询”为目标，不预先承诺所有 provider 都支持会话控制。

## 跨层契约

各层通过稳定公共契约协作：

- CLI 命令及其 JSON 结果使用统一、可版本化的 envelope；
- 写操作遵守检查、计划、确认、执行、验证和提交模型；
- 外部效果必须验证可观测后置条件，失败时回滚或报告保留效果；
- Session 层通过 Workspace 公共服务或 CLI 契约访问项目和授权能力；
- provider 事件使用可版本化的事件契约，并区分 provider 事实与系统推断；
- 各层不得共享未声明的内部存储结构，也不得越层直接写入其他层的数据。

## OpenSpec 边界

OpenSpec 是独立的规格和变更管理体系，不属于上述三层中的任何一层。

- Code Workspace 可以承载使用 OpenSpec 的项目，但不隐式绑定 OpenSpec；
- Workspace 初始化、更新、校验和会话管理不依赖其他 OpenSpec 包或可执行文件；
- Workspace 和 Session 层不读取或修改注册项目的 `openspec/` 内容；
- OpenSpec 变更由用户或明确调用的外部工具负责。

## 当前实现映射

- Foundation 基础能力已经存在，但部分代码仍与 Workspace 领域实现混合；
- Workspace 工作区层已经具备项目注册、查询、授权、校验和安全分支协调等主要能力；
- Session 会话层处于提案阶段；现有 monitor 作为观测原型，为后续 Session 领域模型和事件契约提供基础；
- 后续演进优先建立清晰的模块和公共契约，再根据维护和发布需求决定是否进行物理拆包。
