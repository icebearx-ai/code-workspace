const { interpolate } = require("./interpolate");
const { DEFAULT_LANGUAGE_CODE, LANGUAGE_CODES, LANGUAGE_OPTIONS, LOCALES, getLocale } = require("./registry");

module.exports = {
  DEFAULT_LANGUAGE_CODE,
  LANGUAGE_CODES,
  LANGUAGE_OPTIONS,
  LOCALES,
  getLocale,
  interpolate,
};
