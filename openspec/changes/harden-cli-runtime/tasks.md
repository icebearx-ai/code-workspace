## 1. 命令契约

- [x] 1.1 增加声明式命令注册表，覆盖命令路径、位置参数元数、类型化选项、工作区要求和配置依赖。
- [x] 1.2 使用语义化命令解析替换通用解析器，支持选项顺序调整，并拒绝未知或多余输入。
- [x] 1.3 从注册表派生 Help 和命令补全列表，并增加解析器契约测试。

## 2. 配置兼容性

- [x] 2.1 将宽容的配置文档读取与完整标准化分离，并增加 identity、language、monitor 和 projects 投影。
- [x] 2.2 使命令只通过自身声明的配置投影执行，包括独立于工作区的项目检查。
- [x] 2.3 增加受支持旧 Schema 和不受支持未来 Schema 的行为，并补充只读兼容性测试。

## 3. 写入安全与结果

- [x] 3.1 提取统一的工作区文件事务原语，并将 init/update 的快照回滚迁移到该原语。
- [x] 3.2 将计划效果边界应用到项目变更，并确保外部效果经过验证且不会被错误声明为已回滚。
- [x] 3.3 为注册表和配置失败统一结构化命令错误及 JSON 失败信封。

## 4. 契约守卫

- [x] 4.1 增加命令与配置的兼容矩阵测试，以及写入失败注入回滚测试。
- [x] 4.2 增加发布包内 Skill/README 命令引用与注册表的一致性检查。
- [x] 4.3 运行全量测试、补丁一致性检查和发布包验证。

## 5. 稳定结果与命令边界

- [x] 5.1 引入统一 CommandResult 与文本/JSON renderer，使所有 JSON 成功和失败响应使用同一信封。
- [x] 5.2 将 JSON 模式定义为非交互执行，为缺少确认、业务输入和配置错误补齐稳定错误码。
- [x] 5.3 将 project 等命令处理器拆分到独立模块，并从注册表元数据生成 Help 与 completion。

## 6. 配置、工具与 Doctor

- [x] 6.1 将当前配置提升到 v2，并实现 v0/v1 纯函数迁移注册表、只读兼容投影和维护命令迁移提交。
- [x] 6.2 实现显式参数、state、manifest 默认值的统一工具解析器，并让 init/update/Doctor 使用同一来源。
- [x] 6.3 将 Doctor 改为逐域宽容诊断，保留有效 projects、monitor 和 identity，避免空配置次生错误。

## 7. 效果安全

- [x] 7.1 扩展文件事务报告，精确记录 restored、removed、retained 与 rollbackFailures。
- [x] 7.2 为 OpenSpec 安装、上游 init 和 workspace dependencies 增加命令专属后置验证及不可补偿效果报告。

## 8. 完整验收矩阵

- [x] 8.1 补齐参数边界、旧配置跨命令、统一输出、工具保持和 Doctor 降级测试。
- [x] 8.2 增加 init/update/project 逐阶段故障注入，并验证最终文件系统及效果报告。
- [x] 8.3 强化 Skill/README 契约，使用真实 parser 验证示例并检查 Skill 依赖的 JSON 字段。
- [x] 8.4 运行 OpenSpec 验证、全量测试、补丁一致性和发布包检查，确认所有新增任务完成。
