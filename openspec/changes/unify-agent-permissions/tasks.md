## 1. 建立权限架构

- [x] 1.1 定义规范目录处理、明确的 grant/revoke 请求类型、通用的逐工具计划与结果字段，以及稳定的权限错误码。
- [x] 1.2 增加权限适配器注册表，使权限能力的解析独立于 Workspace 资产工具支持，并拒绝没有适配器的选中工具。
- [x] 1.3 实现通用权限检查和规划，包括受影响目标、前置条件指纹、无变更分类以及确定性的合并变更。
- [x] 1.4 实现通用的事务化应用和验证，在应用失败、计划过期或后置条件失败时回滚所有适配器目标。

## 2. 实现 Agent 权限适配器

- [x] 2.1 将现有 Codex 权限解析和托管块序列化重构到标准适配器接口之后，同时保持无关 TOML 不变。
- [x] 2.2 将 Codex 规划改为应用明确的授权和撤销操作，使只补授权时能够保留额外托管根目录。
- [x] 2.3 实现 Claude `.claude/settings.local.json` 的设置检查和结构验证，并保留无关 JSON 属性。
- [x] 2.4 实现 Claude `permissions.additionalDirectories` 的授权与撤销持久化，包括稳定顺序、去重、原子写入，且不记录所有权元数据。
- [x] 2.5 增加适配器契约测试，覆盖幂等性、路径规范化、配置保留、无效配置、计划过期、验证失败和回滚。

## 3. 替换独立 CLI 命令

- [x] 3.1 注册 `permissions apply`，声明 `projects` 配置域、必需确认、`--tools` 和 `--yes`，并通过职责聚焦的 permissions 命令模块进行路由。
- [x] 3.2 删除 `sync` 注册项、分发路由、处理函数、帮助条目、补救文本，以及将其视为有效命令的测试。
- [x] 3.3 在文本模式下渲染一个合并授权计划，并为所有选中工具返回通用、稳定的 JSON 结果。
- [x] 3.4 确保无变更计划不提示；JSON 或非 TTY 模式下存在写入且未提供 `--yes` 时，在写入前返回 `CLI_CONFIRMATION_REQUIRED`。
- [x] 3.5 从注册表派生重命名后命令及其工具和确认选项的 Bash、Zsh 补全内容。

## 4. 集成项目与生命周期流程

- [x] 4.1 重构 project add，将项目记录和多工具授权一同规划，只确认一次，并在一个事务中提交配置与权限。
- [x] 4.2 重构 project remove，将项目记录删除和明确的多工具撤销一同规划，只确认一次，并在失败时回滚所有文件。
- [x] 4.3 更新初始化和重新初始化流程，只将缺失权限纳入已确认初始化计划，同时保留额外授权。
- [x] 4.4 保持 update 授权中立，将权限文件排除在普通 update 写入之外，并在新选中工具缺少项目授权时提供可操作的补救说明。
- [x] 4.5 扩展 doctor，检查每个选中的权限适配器，报告已注册项目缺失的访问权限，并忽略额外授权目录。

## 5. 验证契约与迁移

- [x] 5.1 为 `permissions apply`、工具选择、确认流程以及 Workspace 发现前拒绝 `sync` 增加解析器、帮助、补全、文本、JSON 和文档引用测试。
- [x] 5.2 增加跨工具集成测试，覆盖只补授权、删除项目时明确撤销、重新初始化时保留授权、update 授权中立以及 doctor 诊断。
- [x] 5.3 在每个项目、配置、适配器写入和验证边界增加故障注入测试，并断言文件系统完全回滚。
- [x] 5.4 更新 README、本地化用户指南、模板、打包的技能说明、manifest 哈希以及补救示例，将 `sync` 替换为 `permissions apply`。
- [x] 5.5 运行 `node scripts/check-cli-architecture.js`、`npm test` 和 `npm run check`；解决全部架构、行为、文档和打包问题，不得削弱检查规则。
