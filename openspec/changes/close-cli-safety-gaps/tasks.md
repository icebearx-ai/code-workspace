## 1. 外部效果核算

- [x] 1.1 增加小型 retained-effect 合并辅助函数，规范 `kind`、`status`、目标范围、证据和 remediation，且不引入新的效果载荷顶层结构。
- [x] 1.2 调整全局 OpenSpec 安装流程，使安装命令执行后的检测失败也携带全局包 retained effect 和观测版本。
- [x] 1.3 调整 workspace dependency 安装流程，在命令失败或依赖后置验证失败时报告 `node_modules`/lockfile 的范围级保留效果。
- [x] 1.4 统一 `openspec init` 直接失败与后置条件失败的效果报告，列出已创建的必需目标并标记枚举范围不完整。
- [x] 1.5 增加上述三条路径的故障注入测试，同时验证工作区文件回滚与外部 retained effect 分开报告。

## 2. 可审计维护迁移

- [x] 2.1 增加维护迁移计划组合器，复用现有 Schema 计划和语言来源解析，输出步骤、来源、冲突和写入目标。
- [x] 2.2 让 init/update 使用组合计划并在现有文件事务中提交，保持只读配置投影无写入。
- [x] 2.3 补充 v0/v1 旧配置的交互式 init、`init --yes`、update 和冲突回归测试，验证迁移输出与失败前零写入。

## 3. 关键命令与写入契约

- [x] 3.1 让命令解析完成后的选项/参数错误携带规范化命令路径，并由统一结果信封填充 `command`。
- [x] 3.2 为权限单文件同步增加写后内容验证和稳定失败代码，不引入多文件事务包装。
- [x] 3.3 增加已识别命令解析失败、未知命令和权限验证失败的代表性 JSON/退出码测试；不扩展为所有命令的文本快照矩阵。

## 4. 验收与停止条件

- [x] 4.1 运行全量 Node 测试，确认外部失败、旧配置和现有 CLI 集成测试全部通过。
- [x] 4.2 运行 Patch 一致性检查和 `npm pack --dry-run`，确认发布内容与托管输出一致。
- [x] 4.3 严格校验 `close-cli-safety-gaps` OpenSpec change。
- [x] 4.4 对照设计的 Non-Goals 审查最终 diff；不拆分 project handler、不增加运行时依赖、不推广未被本变更使用的抽象，满足验收后停止优化。
