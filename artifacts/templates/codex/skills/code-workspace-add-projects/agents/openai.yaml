interface:
  display_name: "Code Workspace Add Projects"
  short_description: "注册新的 Git 项目（被动触发）"
  default_prompt: "Use $code-workspace-add-projects to inspect and register these local project paths."
policy:
  allow_implicit_invocation: false
